name: Daily Streak

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  streak:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          date >> .streak
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .streak
          git commit -m "chore: streak $(date +%Y-%m-%d)"
          git push
